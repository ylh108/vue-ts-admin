import request from '@/http'
import config from '@/config'

const login = {
    treeSelect () {
        return request({
            url: '',
            data: ''
        })
    }
}
export default login
